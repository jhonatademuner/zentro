/**
 * Core barrel export
 *
 * Public API of the framework-agnostic domain layer.
 * Re-export shared types and utilities here as they are created.
 */

// export { HttpExecutor } from './executors/http.executor.ts'; for example
